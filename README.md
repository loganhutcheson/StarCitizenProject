Project is using Json.NET to parse the responses. To install with Visual Studio Code run terminal: dotnet add package Newtonsoft.Json     --  To install with Visual Code you can use the Nuget package searcher
